[pytest]
addopts = -q
testpaths = tests
markers =
    lite: tiny synthetic IO tests (no external data)
    full: requires external datasets or network
    playwright: browser smoke tests for the built documentation site
